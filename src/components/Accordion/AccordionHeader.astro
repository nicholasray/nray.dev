---
import type { HTMLTag, Polymorphic } from "astro/types";

interface Props extends Polymorphic<{ as: HTMLTag }> {
  as: HTMLTag;
  className?: string;
}

const { className, as: Tag } = Astro.props;
---

<Tag class:list={["accordion-header", className]}>
  <slot />
</Tag>
